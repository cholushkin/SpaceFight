#ifndef config_h__
#define config_h__


// makes OpenGL not to rotate view so game has to resize window and handle input events in the right way ($$$ refactoring will be needed for that)
//#define WIN32_LANDSCAPE

// Enable profiling. Define this macro in client code
// #define PROFILER_ENABLED

// makes profiler sample average times over specified number of frames. Define this macro in client code
// #define SMOOTH_PROFILER 2


#endif // config_h__
